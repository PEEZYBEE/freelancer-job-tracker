#!/usr/bin/env python3

from models import session, User, Client, Job, Payment
import ipdb

# 🌱 Add sample queries or test instances here if desired

print("\n✅ Loaded. Type `exit` or press Ctrl+D to quit.")
ipdb.set_trace()
